'use client';
import { useRouter, usePathname } from 'next/navigation';

// FloatingSOS is disabled — SOS is accessible from the header SOS button
// and the drawer nav. A position:fixed element would escape the phone frame.
export default function FloatingSOS() {
  return null;
}
